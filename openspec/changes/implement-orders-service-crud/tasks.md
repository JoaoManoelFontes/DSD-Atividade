## 1. Persistence Setup

- [x] 1.1 Add Drizzle ORM and PostgreSQL dependencies and scripts to orders-service
- [x] 1.2 Add the orders database schema, client, migration runner, and generated migration
- [x] 1.3 Run pending orders database migrations before the Docker container starts

## 2. Orders CRUD and Menu Communication

- [x] 2.1 Add orders-service configuration for PostgreSQL and the menu-service URL
- [x] 2.2 Add a small HTTP client that retrieves and validates an available menu item
- [x] 2.3 Implement create, list, retrieve, update, and delete order endpoints

## 3. Documentation and Verification

- [x] 3.1 Document orders-service setup, domain fields, endpoints, and menu-service dependency
- [x] 3.2 Update the microservices overview with the orders-service capability
- [x] 3.3 Build orders-service and validate the generated OpenSpec change
